<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ChatMessageMapper">

    <select id="selectBySessionIdOrderByTime" resultType="com.example.entity.ChatMessage">
        SELECT m.*,
               CASE WHEN f.id IS NOT NULL THEN 1 ELSE 0 END AS favorite
        FROM chat_message m
                 LEFT JOIN chat_feedback f
                           ON m.id = f.message_id
                               and f.feedback_type = 'like'
                               AND f.is_delete = 0
        WHERE m.session_id = #{sessionId}
          AND m.is_delete = 0
        ORDER BY m.create_time ASC
    </select>

</mapper>